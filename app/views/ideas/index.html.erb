<table>
  <thead>
    <tr>
      <th>Autor</th>
      <th>Cena</th>
      <th>Tytul</th>
      <th>

      </th>
    </tr>
  </thead>
  <tbody>
      <% @ideas.each do |idea| %>
        <tr>
          <td><%= link_to idea.author, idea_path(idea)  %></td>
          <td><%= idea.price %></td>
          <td><%= idea.title %></td>
          <td>
            <%= button_to "usun", idea_path(idea),
              method: :delete, class: "delete" %>
          </td>
        </tr>
      <% end %>
  </tbody>
</table>
